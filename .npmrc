# Zie package.json scripts voor NODE_OPTIONS="--max-old-space-size=4096"
# De max_old_space_size optie is geen geldige npm config key;
# deze moet via de NODE_OPTIONS environment variable worden doorgegeven.
