{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun install",
  "buildCommand": "NODE_OPTIONS='--max-old-space-size=4096' bunx prisma generate && NODE_OPTIONS='--max-old-space-size=4096' next build",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/sync",
      "schedule": "0 3 * * *"
    }
  ]
}
